# static.mod

Serves static files.

## Configuration

    "static":{
      "root":"static",
      "maxAge":60
    }

 * *root* - where the document-root is
 * *maxAge* - seconds in the future the *Expires* header is set to
